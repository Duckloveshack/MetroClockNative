import { StyleSheet } from "react-native";

const FontStyles = StyleSheet.create({
    title: {
        fontWeight: 200,
        fontSize: 60
    },
    sectionTitle: {
        fontSize: 16,
        fontWeight: 600
    },
    info: {
        fontSize: 18,
        fontWeight: 400
    }
});

export default FontStyles;